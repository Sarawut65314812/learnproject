# 🚀 วิธีเปิดโปรเจกต์ใหม่ทุกครั้ง

คู่มือสำหรับเปิดโปรเจกต์หลังจากปิดคอมพิวเตอร์หรือปิดโปรแกรม

---

## ✅ ขั้นตอนการเปิดโปรเจกต์ (3 ขั้นตอนง่ายๆ)

### ขั้นตอนที่ 1: เปิด Backend Server

```powershell
# เปิด PowerShell แล้วไปที่โฟลเดอร์ backend
cd C:\Users\asus\Desktop\projectonline\backend

# เริ่ม Backend Server
npm start
```

**ผลลัพธ์ที่ต้องเห็น:**
```
🚀 Server is running on port 5000
✅ MongoDB connected successfully
```

⚠️ **อย่าปิด Terminal นี้** - ปล่อยให้รันไว้ตลอด

---

### ขั้นตอนที่ 2: เปิด Admin Panel

**เปิด PowerShell หน้าต่างใหม่** (อีกหน้าต่างนึง) แล้วรัน:

```powershell
# ไปที่โฟลเดอร์ admin
cd C:\Users\asus\Desktop\projectonline\admin

# เริ่ม Admin Panel
npm start
```

**ผลลัพธ์:**
- จะเปิดเบราว์เซอร์อัตโนมัติที่ `http://localhost:3000`
- เห็นหน้า Login

**ข้อมูลเข้าสู่ระบบ:**
- Email: `admin@example.com`
- Password: `admin123`

⚠️ **อย่าปิด Terminal นี้** - ปล่อยให้รันไว้ตลอด

---

### ขั้นตอนที่ 3: เปิด Frontend (หน้าเว็บหลัก)

**วิธีที่ 1: เปิดด้วย PowerShell**
```powershell
start C:\Users\asus\Desktop\projectonline\frontend\index.html
```

**วิธีที่ 2: เปิดด้วย File Explorer**
1. ไปที่โฟลเดอร์ `C:\Users\asus\Desktop\projectonline\frontend`
2. คลิกขวาที่ไฟล์ `index.html`
3. เลือก "Open with" → เลือก Browser (Chrome, Edge, Firefox)

---

## 📝 สรุปแบบสั้น

**เปิด 2 Terminal:**

**Terminal 1 - Backend:**
```powershell
cd C:\Users\asus\Desktop\projectonline\backend
npm start
```

**Terminal 2 - Admin:**
```powershell
cd C:\Users\asus\Desktop\projectonline\admin
npm start
```

**Frontend:**
- เปิดไฟล์ `frontend/index.html` ในเบราว์เซอร์

---

## 🔍 วิธีตรวจสอบว่าทำงานถูกต้อง

### ✅ Backend ทำงาน:
- เปิด browser ไปที่: `http://localhost:5000/api/health`
- ต้องเห็น: `{"status":"OK","message":"Server is running"}`

### ✅ Admin Panel ทำงาน:
- เปิด browser ไปที่: `http://localhost:3000`
- ต้องเห็นหน้า Login สีม่วง

### ✅ Frontend ทำงาน:
- เปิดไฟล์ `frontend/index.html`
- ต้องเห็นสินค้าแสดงผล (ถ้ามีสินค้าในระบบ)

---

## ❌ แก้ปัญหาที่พบบ่อย

### ปัญหา 1: Port 5000 ถูกใช้งานอยู่
```
Error: listen EADDRINUSE: address already in use :::5000
```

**วิธีแก้:**
```powershell
# ดูว่า Process ไหนใช้ Port 5000
netstat -ano | findstr :5000

# ปิด Process (แทน PID ด้วยตัวเลขที่เจอ)
taskkill /PID <PID> /F
```

หรือ **รีสตาร์ทคอมพิวเตอร์**

---

### ปัญหา 2: MongoDB ไม่เชื่อมต่อ
```
❌ MongoDB connection error
```

**วิธีแก้:**
1. ตรวจสอบว่าอินเทอร์เน็ตเชื่อมต่ออยู่
2. ตรวจสอบไฟล์ `.env` ในโฟลเดอร์ `backend`:
   ```
   MONGODB_URI=mongodb+srv://sarawutp65:admin123@cluster0.f7rnt98.mongodb.net/projectonline?retryWrites=true&w=majority
   ```
3. ลอง flush DNS:
   ```powershell
   ipconfig /flushdns
   ```

---

### ปัญหา 3: Admin Panel ไม่เปิด
```
Something is already running on port 3000
```

**วิธีแก้:**
- กด `Y` เมื่อถามว่าจะใช้ port อื่นไหม
- หรือปิดโปรแกรมที่ใช้ port 3000 อยู่

---

### ปัญหา 4: Frontend ไม่แสดงสินค้า
```
⚠️ Error Loading Items
```

**วิธีแก้:**
1. ตรวจสอบว่า Backend รันอยู่ไหม (Terminal ต้องยังเปิดอยู่)
2. ตรวจสอบว่ามีสินค้าในระบบหรือยัง:
   - เปิด Admin Panel
   - ไปที่ "Manage Items"
   - เพิ่มสินค้าใหม่
3. รีเฟรชหน้า Frontend (F5)

---

## 🛑 วิธีหยุด/ปิดโปรเจกต์

### หยุด Backend:
- ไปที่ Terminal ที่รัน Backend
- กด `Ctrl + C`

### หยุด Admin Panel:
- ไปที่ Terminal ที่รัน Admin Panel
- กด `Ctrl + C`

### ปิด Frontend:
- ปิด Tab ในเบราว์เซอร์

---

## 📌 ข้อมูลสำคัญที่ต้องจำ

### ข้อมูล Admin:
- **Email**: `admin@example.com`
- **Password**: `admin123`

### ข้อมูล MongoDB Atlas:
- **Username**: `sarawutp65`
- **Password**: `admin123`
- **Cluster**: `cluster0.f7rnt98.mongodb.net`

### URLs:
- **Backend API**: `http://localhost:5000`
- **Admin Panel**: `http://localhost:3000`
- **Frontend**: เปิดไฟล์ `frontend/index.html`

---

## 🎯 Checklist ก่อนเริ่มทำงาน

- [ ] เปิด Backend Server (Terminal 1)
- [ ] รอจนเห็น "✅ MongoDB connected successfully"
- [ ] เปิด Admin Panel (Terminal 2)
- [ ] รอจนเบราว์เซอร์เปิดหน้า Login
- [ ] เปิด Frontend ในเบราว์เซอร์
- [ ] ทดสอบว่าทุกอย่างทำงานปกติ

---

## 💡 เคล็ดลับ

### เปิดทั้งหมดพร้อมกัน:

สร้างไฟล์ `start.bat` ในโฟลเดอร์หลัก:

```batch
@echo off
echo Starting Backend...
start cmd /k "cd backend && npm start"

timeout /t 3

echo Starting Admin Panel...
start cmd /k "cd admin && npm start"

timeout /t 3

echo Opening Frontend...
start frontend\index.html

echo Done! All services started.
```

จากนั้นคลิกไฟล์ `start.bat` ทุกครั้งที่ต้องการเปิดโปรเจกต์!

---

## 📞 ต้องการความช่วยเหลือ?

ถ้ามีปัญหา:
1. ตรวจสอบว่า Terminal แสดง Error อะไร
2. ลองรีสตาร์ท Backend และ Admin Panel
3. ตรวจสอบว่าอินเทอร์เน็ตเชื่อมต่ออยู่
4. อ่าน `README.md` สำหรับข้อมูลเพิ่มเติม

---

**สนุกกับการใช้งานนะครับ! 🚀**
