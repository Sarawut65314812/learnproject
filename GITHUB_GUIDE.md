# 📤 วิธีอัพโหลดโปรเจกต์ขึ้น GitHub

คู่มือสำหรับการอัพโหลดโค้ดขึ้น GitHub แบบง่ายๆ

---

## 📋 เตรียมความพร้อม

### 1. ติดตั้ง Git

ดาวน์โหลดและติดตั้ง Git:
- เข้าไปที่: https://git-scm.com/download/win
- ดาวน์โหลดและติดตั้งตามปกติ
- รีสตาร์ท PowerShell หลังติดตั้ง

### 2. ตรวจสอบว่าติดตั้งสำเร็จ

```powershell
git --version
```

ต้องเห็นเลข version เช่น: `git version 2.43.0`

---

## 🌐 สร้าง GitHub Account

1. ไปที่: https://github.com/
2. กด "Sign up"
3. กรอกข้อมูล:
   - Email
   - Password
   - Username (ตั้งชื่อที่ชอบ)
4. ยืนยัน Email
5. เสร็จแล้ว!

---

## 🚀 ขั้นตอนอัพโหลดโปรเจกต์

### ขั้นตอนที่ 1: ตั้งค่า Git ครั้งแรก

เปิด PowerShell:

```powershell
# ตั้งชื่อ (ใช้ชื่อจริงหรือชื่อเล่น)
git config --global user.name "Your Name"

# ตั้ง Email (ต้องตรงกับ GitHub)
git config --global user.email "your.email@example.com"

# ตรวจสอบ
git config --list
```

---

### ขั้นตอนที่ 2: เตรียมโปรเจกต์

```powershell
# ไปที่โฟลเดอร์โปรเจกต์
cd C:\Users\asus\Desktop\projectonline

# สร้าง Git repository
git init
```

**ผลลัพธ์:** เห็นข้อความ `Initialized empty Git repository`

---

### ขั้นตอนที่ 3: ตรวจสอบไฟล์ .gitignore

ตรวจสอบว่ามีไฟล์ `.gitignore` แล้ว (มีอยู่แล้วในโปรเจกต์)

เนื้อหาควรมี:
```
node_modules/
.env
*.log
build/
dist/
.DS_Store
```

**สำคัญ:** ไฟล์นี้บอก Git ว่าอย่า upload อะไรบ้าง
- `node_modules/` - ขนาดใหญ่มาก ไม่ต้อง upload
- `.env` - มีรหัสผ่าน ห้าม upload!

---

### ขั้นตอนที่ 4: Add และ Commit ไฟล์

```powershell
# เพิ่มไฟล์ทั้งหมด
git add .

# ตรวจสอบว่าไฟล์อะไรจะถูก upload
git status

# Commit (บันทึก)
git commit -m "Initial commit - Project Online Full Stack"
```

**หมายเหตุ:** ถ้าเห็น warning เรื่อง line endings (CRLF/LF) ไม่เป็นไร ปล่อยไว้ได้

---

### ขั้นตอนที่ 5: สร้าง Repository บน GitHub

1. ไปที่ https://github.com/
2. กดปุ่ม **"+"** ด้านบนขวา → เลือก **"New repository"**
3. กรอกข้อมูล:
   - **Repository name**: `projectonline`
   - **Description**: `Full-stack project with Node.js, React, and MongoDB`
   - **Public** หรือ **Private**: เลือกตามใจ
   - ❌ **ห้ามติ๊ก** "Add a README file"
   - ❌ **ห้ามเลือก** .gitignore และ license
4. กด **"Create repository"**

---

### ขั้นตอนที่ 6: เชื่อมต่อกับ GitHub

หลังกด Create จะเห็นหน้าคำสั่ง ให้ใช้คำสั่งนี้:

```powershell
# เชื่อมต่อกับ GitHub (แทน YOUR_USERNAME ด้วย username ของคุณ)
git remote add origin https://github.com/YOUR_USERNAME/projectonline.git

# เปลี่ยน branch เป็น main
git branch -M main

# Upload!
git push -u origin main
```

**ครั้งแรกจะถาม Username และ Password:**

---

### ⚠️ สำคัญ! การ Login GitHub

GitHub ไม่ให้ใช้รหัสผ่านปกติแล้ว ต้องใช้ **Personal Access Token**

#### วิธีสร้าง Token:

1. ไปที่ GitHub → คลิกรูปโปรไฟล์ → **Settings**
2. เลื่อนลงล่างสุด → **Developer settings**
3. **Personal access tokens** → **Tokens (classic)**
4. **Generate new token** → **Generate new token (classic)**
5. ตั้งค่า:
   - **Note**: `My PC - Project Online`
   - **Expiration**: `90 days` หรือ `No expiration`
   - **Select scopes**: ✅ ติ๊ก `repo` (ทั้งหมด)
6. กด **Generate token**
7. **คัดลอก Token ทันที!** (เห็นครั้งเดียว)

#### ใช้ Token แทนรหัสผ่าน:

เมื่อ `git push` ถาม:
```
Username: YOUR_USERNAME
Password: [วาง Token ที่คัดลอกมา]
```

**จด Token ไว้ดีๆ!** จะต้องใช้ทุกครั้งที่ push

---

### ขั้นตอนที่ 7: ตรวจสอบบน GitHub

1. Refresh หน้า Repository บน GitHub
2. จะเห็นไฟล์โปรเจกต์ทั้งหมด
3. เสร็จสิ้น! ✅

---

## 🔄 อัพเดทโค้ดในอนาคต

เมื่อแก้ไขโค้ด:

```powershell
# เพิ่มไฟล์ที่แก้ไข
git add .

# Commit พร้อมข้อความ
git commit -m "Add: เพิ่มฟีเจอร์อะไรก็ได้"

# Push ขึ้น GitHub
git push
```

**ตัวอย่างข้อความ Commit:**
```
git commit -m "Fix: แก้ไข bug ใน login"
git commit -m "Update: ปรับปรุง UI Admin Panel"
git commit -m "Add: เพิ่มระบบค้นหาสินค้า"
```

---

## 📝 คำสั่ง Git ที่ใช้บ่อย

### ดูสถานะ:
```powershell
git status
```

### ดูประวัติ:
```powershell
git log --oneline
```

### ดู Remote URL:
```powershell
git remote -v
```

### ยกเลิก Add:
```powershell
git reset
```

### ดู Changes:
```powershell
git diff
```

---

## 🔐 ความปลอดภัย

### ✅ สิ่งที่ควรทำ:

1. **ใส่ `.gitignore`** - อย่า upload `node_modules` และ `.env`
2. **ตรวจสอบก่อน Push**:
   ```powershell
   git status
   ```
3. **ไม่ให้มี `.env` ใน Git**:
   ```powershell
   git rm --cached .env
   git rm --cached backend/.env
   ```

### ❌ สิ่งที่ห้ามทำ:

- ❌ อย่า commit ไฟล์ `.env` (มีรหัสผ่าน!)
- ❌ อย่า commit `node_modules` (ไฟล์เยอะมาก)
- ❌ อย่าเผย Token/API Keys ใน code

---

## 🆘 แก้ปัญหาที่พบบ่อย

### ปัญหา 1: Upload ไม่ได้ (Authentication failed)

**สาเหตุ:** ใช้รหัสผ่านปกติ  
**วิธีแก้:** ใช้ Personal Access Token แทน (ดูด้านบน)

---

### ปัญหา 2: ไฟล์ที่ไม่ต้องการถูก commit

```powershell
# ลบออกจาก Git (แต่ยังอยู่ในเครื่อง)
git rm --cached filename

# หรือลบทั้งโฟลเดอร์
git rm -r --cached node_modules/

# Commit
git commit -m "Remove unnecessary files"
git push
```

---

### ปัญหา 3: ต้องการยกเลิก commit ล่าสุด

```powershell
# ยกเลิก แต่เก็บการแก้ไขไว้
git reset --soft HEAD~1

# ยกเลิก และลบการแก้ไขทิ้ง
git reset --hard HEAD~1
```

---

### ปัญหา 4: Repository มีอยู่แล้ว

```powershell
# ลบ remote เดิม
git remote remove origin

# เพิ่ม remote ใหม่
git remote add origin https://github.com/YOUR_USERNAME/projectonline.git

# Push
git push -u origin main
```

---

## 📱 ทางเลือกอื่น: ใช้ GitHub Desktop

ถ้าไม่ชอบใช้คำสั่ง สามารถใช้โปรแกรมแทนได้:

1. ดาวน์โหลด: https://desktop.github.com/
2. ติดตั้งและ Login
3. **File** → **Add Local Repository** → เลือกโฟลเดอร์ `projectonline`
4. กด **Publish repository**
5. เสร็จแล้ว! ใช้ GUI แทนคำสั่ง

---

## ✅ Checklist ก่อน Push

- [ ] มีไฟล์ `.gitignore` แล้ว
- [ ] ไม่มีไฟล์ `.env` ใน Git
- [ ] ไม่มีโฟลเดอร์ `node_modules/` ใน Git
- [ ] รัน `git status` ดูว่าจะ upload อะไรบ้าง
- [ ] เขียนข้อความ commit ชัดเจน
- [ ] คัดลอก Personal Access Token ไว้แล้ว

---

## 🎯 คำสั่งย่อ (ใช้บ่อย)

```powershell
# 1. ไปที่โปรเจกต์
cd C:\Users\asus\Desktop\projectonline

# 2. เพิ่มไฟล์ที่แก้ไข
git add .

# 3. Commit
git commit -m "Update: อธิบายสิ่งที่แก้ไข"

# 4. Push
git push
```

---

## 📚 เรียนรู้เพิ่มเติม

- Git Documentation: https://git-scm.com/doc
- GitHub Guides: https://guides.github.com/
- Git Cheat Sheet: https://education.github.com/git-cheat-sheet-education.pdf

---

## 🎉 สรุป

### ครั้งแรก:
1. ติดตั้ง Git
2. สร้าง GitHub Account
3. ตั้งค่า Git (`git config`)
4. สร้าง Repository บน GitHub
5. เชื่อมต่อและ Push (`git push`)

### ครั้งถัดไป:
```powershell
git add .
git commit -m "ข้อความ"
git push
```

**ง่ายมากครับ!** 🚀

---

**พร้อมที่จะลองแล้วหรือยัง? มีคำถามอะไรถามได้เลยนะครับ!** 😊
